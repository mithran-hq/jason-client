# Contributing

This repository preserves the public Jason client boundary. Do not add scheduler,
controller, worker, runtime-control, or credential-materialization code here.

Before sending changes, run:

```sh
cargo test
```
